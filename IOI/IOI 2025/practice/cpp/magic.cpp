#include "magic.h"
#include <bits/stdc++.h>
using namespace std;

vector<int> Alicia(vector<int> P) {
	for(int i = 
}

vector<int> Beatriz(vector<int> Q) {
  	int N = (int)Q.size();
  	return vector<int>(N, 0);
}

